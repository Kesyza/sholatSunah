import 'package:flutter/material.dart';

class SunahDuha extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Sholat Sunah Duha'),
      ),
    );
  }
}
